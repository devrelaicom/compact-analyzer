# The published VSIX ships only the bundled output (dist/) plus static
# assets and metadata. Everything listed here is excluded from the package.
# The runtime dependency (vscode-languageclient) is inlined into dist/ by
# esbuild, so node_modules must NOT be shipped.

# TypeScript sources and tests (compiled into dist/extension.js)
src/**

# Development-only language-asset inputs and audit notes. The runtime assets
# (language-configuration.json, syntaxes/, snippets/) live outside assets/ and
# ARE shipped; only the regeneration inputs and notes are excluded here.
assets/**

# Tooling and configuration
tsconfig.json
esbuild.mjs
eslint.config.mjs
.gitignore
.vscodeignore
.nvmrc

# E2E test harness: config, compiled test output, and the downloaded-VS-Code
# cache. The e2e TypeScript sources live under src/** (already excluded above);
# these are the extra artefacts the harness adds. The @vscode/test-* devDeps are
# never packaged because node_modules/ is excluded below.
.vscode-test.mjs
vitest.config.ts
out/**
.vscode-test/**

# Dependencies and lockfile
node_modules/**
package-lock.json
