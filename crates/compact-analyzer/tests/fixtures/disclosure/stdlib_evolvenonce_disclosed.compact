pragma language_version >= 0.23;
import CompactStandardLibrary;

export ledger stored: Bytes<32>;

witness secret(): Bytes<32>;

export circuit run(): [] {
  stored = disclose(evolveNonce(0, secret()));
}
