pragma language_version >= 0.23;
import CompactStandardLibrary;

export ledger stored: Maybe<Bytes<32>>;

witness secret(): Bytes<32>;

export circuit run(): [] {
  stored = some<Bytes<32>>(secret());
}
