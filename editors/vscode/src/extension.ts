// Extension entry point. This is a stub scaffolded in Task 1; later tasks
// wire up the language client that launches `compact-analyzer` and speaks
// LSP. Activation must never throw.

/** Called by VS Code when the extension is activated. */
export function activate(): void {}

/** Called by VS Code when the extension is deactivated. */
export function deactivate(): void {}
